- hosts: localhost
  gather_facts: no
  tasks:
  - name: Determine hosts
    set_fact:
      nodes: '{{ ( groups["mconf-live220"] + groups["mconf-rec"] + groups["mconf-recw"] ) | sort }}'

  - template:
      src: templates/prometheus-targets.json.j2
      dest: /tmp/targets.json
