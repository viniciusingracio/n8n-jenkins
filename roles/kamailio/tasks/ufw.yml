- name: Configure ufw rules
  become: yes
  command: ufw route allow proto {{ item.proto }} to any port {{ item.port }}
  with_items:
    - { port: 5060, proto: 'tcp' }
    - { port: 5060, proto: 'udp' }
    - { port: 80, proto: 'tcp' }
    - { port: 443, proto: 'tcp' }
  notify:
    - restart ufw
