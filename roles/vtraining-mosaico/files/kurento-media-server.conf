check process kurento-media-server with pidfile "/var/run/kurento-media-server.pid"
    start program = "/usr/sbin/service kurento-media-server-6.0 start"
    stop program = "/usr/sbin/service kurento-media-server-6.0 force-stop"

    if memory > 70% for 9 cycles then restart
    # if memory > 70% for 9 cycles then exec "/usr/local/bin/slack.sh"
