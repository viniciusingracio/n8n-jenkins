engine: {{ asr_engine }}

gcloud_bucket_name: {{ transcribe_gcloud_bucket_name }}
gcloud_credentials_path_asr: {{ transcribe_gcloud_credentials_path_asr }}
gcloud_credentials_path_storage: {{ transcribe_gcloud_credentials_path_storage }}

aws_key: {{ transcribe_aws_key }}
aws_secret: {{ transcribe_aws_secret }}
aws_region: {{ transcribe_aws_region }}
aws_bucket_name: {{ transcribe_aws_bucket_name }}

language_code: pt-BR
language_name: Português (Brasil)
caption_duration: 5.0
caption_paragraph_min_size: 50
{% if asr_matcher | length > 0 %}
matcher:
{{ asr_matcher | to_nice_yaml }}
{% else %}
matcher: []
{% endif %}
