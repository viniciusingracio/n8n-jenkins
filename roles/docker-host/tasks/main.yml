- name: Add docker gpg key
  become: true
  apt_key:
    url: "https://download.docker.com/linux/ubuntu/gpg"
    state: present

- name: Add docker apt repository
  become: true
  apt_repository:
    repo: 'deb [arch=amd64] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable'
    state: present

- name: Update apt
  become: true
  apt: update_cache=yes

- name: Install docker package
  become: true
  apt:
    name: docker-ce
    state: present

# Need pip and docker-py to manage this docker instance with ansible
# pip is now installed on setup.yml
- name: Install docker-py
  become: true
  pip:
    name: docker-py
    state: present

- name: Add user to docker group
  become: true
  user:
    name: '{{ deploy_user }}'
    groups: docker
    append: yes

- name: Log into private registry and force re-authorization
  shell: "docker login -p {{ mconf_registry_password }} -u {{ mconf_registry_user }} {{ mconf_registry_host }}"
  # TODO: can't use docker_login yet because it breaks with python3
  # docker_login:
  #   registry: '{{ mconf_registry_host }}'
  #   username: mconf
  #   password: INCMc0nfito123
  #   reauthorize: yes
