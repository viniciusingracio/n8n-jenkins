- name: Install mcs-sip
  become: true
  apt:
    name: 'mconf-mcs-sip'
    state: '{{ bigbluebutton_apt_state }}'
  notify: restart bigbluebutton

- include_role:
    name: ffmpeg
  tags:
    - ffmpeg
