---

ssl_certificate: /etc/certs/{{ inventory_hostname }}.crt
ssl_certificate_key: /etc/certs/{{ inventory_hostname }}.key

vtraining_database: postgresql # or sqlserver
vtraining_database_name: vtraining_development
vtraining_database_username: vtraining
vtraining_database_password: vtraining
vtraining_database_host: localhost

vtraining_mosaic_url: https://{{ ansible_default_ipv4.address }}:8443/mosaic
