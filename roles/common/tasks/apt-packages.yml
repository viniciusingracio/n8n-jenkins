- name: Update apt
  become: true
  apt: update_cache=yes

- name: Install common packages
  become: true
  apt:
    name: '{{ item }}'
    state: present
  with_items:
    - aptitude
    - curl
    - git
    - wget
    - htop
    - vim
    - tree
    - apt-transport-https
