---

memcached_port: 11211
memcached_listen_address: 0.0.0.0
memcached_max_conn: 1024
memcached_cache_size: 64
memcached_fs_file_max: 756024

memcached_ubuntu_pkgs:
  - python-selinux
  - memcached

memcached_ufw_rules:
# example:
# - { rule: 'allow', port: memcached_port, proto: 'tcp', from_ip: '172.1.0.0' }
