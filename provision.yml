# Common setup for all hosts
- hosts: all
  roles:
  - common

# Recording workers
- hosts: rec-worker
  roles:
  - docker-host
  - rec-worker

# BigBlueButton
- hosts: bigbluebutton
  roles:
  - bigbluebutton
